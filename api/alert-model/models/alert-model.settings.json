{
  "kind": "collectionType",
  "connection": "default",
  "collectionName": "alert_models",
  "info": {
    "name": "AlertModel"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "Element": {
      "type": "string",
      "required": true
    },
    "alerts": {
      "via": "alert_model",
      "collection": "alert"
    }
  }
}
